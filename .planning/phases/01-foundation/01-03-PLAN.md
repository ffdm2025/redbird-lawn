---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .nvmrc
autonomous: false
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-04

user_setup:
  - service: cloudflare-pages
    why: "Static site hosting — DNS already on Cloudflare, free unlimited bandwidth"
    dashboard_config:
      - task: "Create a new Pages project connected to your git repo"
        location: "Cloudflare Dashboard > Workers & Pages > Create application > Pages > Connect to Git"
        details: "Set Build command: npm run build | Build output directory: dist | Node.js version env var: NODE_VERSION = 22"
      - task: "Disable Auto Minify for HTML"
        location: "Cloudflare Dashboard > Your domain > Speed > Optimization > Content Optimization > Auto Minify"
        details: "Uncheck HTML only. Leave CSS and JS unchecked too if shown. This prevents Cloudflare's minifier from corrupting Astro's React hydration payload in Phase 4."
      - task: "Confirm deploy URL ends in .pages.dev (not .workers.dev)"
        location: "Workers & Pages > your project > Deployments tab"
        details: "If the URL shows <hash>.workers.dev, go to project Settings and use the 'Shift to Pages' link to migrate."

must_haves:
  truths:
    - "A `*.pages.dev` URL (not `*.workers.dev`) returns HTTP 200 and serves the scaffold HTML"
    - "Cloudflare Pages build log shows Node.js 22.x"
    - "The deployed page contains 'Expert Residential Lawn Care in Wentzville, MO' from the index.astro placeholder"
    - "Cloudflare Auto Minify for HTML is disabled"
  artifacts:
    - path: ".nvmrc"
      provides: "Node version pin read by Cloudflare Pages build image"
      contains: "22"
  key_links:
    - from: "Cloudflare Pages project"
      to: "git repo"
      via: "GitHub/GitLab integration or direct upload"
      pattern: "deploy triggers on push to main"
---

<objective>
Deploy the scaffold to Cloudflare Pages and verify the `*.pages.dev` URL serves a valid static HTML page. Disable Cloudflare Auto Minify to prevent React hydration corruption in Phase 4.

Purpose: DEPL-01 through DEPL-04 require a live Cloudflare Pages deployment — this is the phase gate. Everything in Phase 2 depends on this URL existing. Deploying now also validates the build pipeline before any real content is added.
Output: Live `*.pages.dev` URL returning HTTP 200 with the scaffold HTML, Node.js 22 confirmed in build log, Auto Minify disabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify final build state and push to git</name>

  <read_first>
    - package.json (confirm build script exists)
    - .nvmrc (confirm contains "22")
    - astro.config.mjs (confirm output: 'static' is still present)
  </read_first>

  <files>
    .nvmrc
  </files>

  <action>
Run a final pre-deploy verification to confirm the project is in deploy-ready state:

```bash
npm run build
npx astro check
```

Both must exit 0.

Confirm `.nvmrc` contains exactly `22` (single line, no extra whitespace). If it does not exist or has wrong content, write it now:

```
22
```

Push all changes to the git repository's main branch so Cloudflare Pages git integration can pick them up. If the project is not yet in a git repo, initialize one:

```bash
git init
git add -A
git commit -m "feat(01-foundation): scaffold Astro 6, Tailwind 4, self-hosted fonts, NAP constants"
```

If the git repo already has a remote (e.g., GitHub), push:
```bash
git push origin main
```
  </action>

  <verify>
    <automated>
      cd "C:/Users/stlto/My Drive (it@redbirdlawnservice.com)/Website Build" && npm run build && npx astro check && cat .nvmrc && echo "PRE-DEPLOY CHECKS PASSED"
    </automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx astro check` exits 0
    - `.nvmrc` contains `22`
    - `astro.config.mjs` contains `output: 'static'`
    - All changes committed to git (clean `git status`)
  </acceptance_criteria>

  <done>Project is in clean, build-passing state with all changes committed. Ready for Cloudflare Pages deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Cloudflare Pages and verify live URL</name>

  <what-built>
    Claude has built and committed a working Astro 6 scaffold with Tailwind 4, self-hosted fonts, and canonical NAP constants. The project builds clean locally.
  </what-built>

  <how-to-verify>
    Complete these steps in order:

    **Step 1 — Connect repo to Cloudflare Pages (if not done already):**
    - Go to: Cloudflare Dashboard > Workers & Pages > Create application > Pages > Connect to Git
    - Select your repository and branch (main)
    - Set build settings:
      - Build command: `npm run build`
      - Build output directory: `dist`
    - Add environment variable: `NODE_VERSION` = `22`
    - Click Save and Deploy

    **Step 2 — Confirm the deploy URL format:**
    - Go to: Workers & Pages > your project > Deployments tab
    - The deployment URL MUST end in `.pages.dev` (e.g., `redbird-lawn.pages.dev`)
    - If it shows `.workers.dev` instead: go to project Settings > find "Shift to Pages" link > click it

    **Step 3 — Verify the live page:**
    - Open the `.pages.dev` URL in a browser
    - Confirm the page loads (not a 404 or error)
    - Confirm the H1 "Expert Residential Lawn Care in Wentzville, MO" appears

    **Step 4 — Check Node.js version in build log:**
    - In Cloudflare Pages project > Deployments > click the deployment > View build log
    - Confirm the log contains `Node.js v22` (e.g., `Installing Node.js v22.x.x`)

    **Step 5 — Disable Auto Minify for HTML:**
    - Go to: Cloudflare Dashboard > your domain (redbirdlawnservice.com) > Speed > Optimization > Content Optimization
    - Find "Auto Minify" — uncheck HTML (and CSS/JS if shown)
    - Save

    **Expected outcome:**
    - `https://<name>.pages.dev` returns HTTP 200 with the scaffold page
    - Build log shows Node.js 22.x
    - Auto Minify HTML is disabled
  </how-to-verify>

  <resume-signal>
    Confirm by typing "deployed" along with the `.pages.dev` URL (e.g., "deployed: redbird-lawn.pages.dev"). If there were issues, describe what happened instead.
  </resume-signal>
</task>

</tasks>

<verification>
After human confirms deployment:
1. `curl -s -o /dev/null -w "%{http_code}" https://<name>.pages.dev` — must return `200`
2. `curl -s https://<name>.pages.dev | grep "Expert Residential"` — must return the H1 text
3. Build log in Cloudflare Dashboard confirms Node.js 22.x
4. Auto Minify HTML confirmed disabled in Cloudflare Speed settings
</verification>

<success_criteria>
- `*.pages.dev` URL (not `*.workers.dev`) returns HTTP 200
- Page source contains "Expert Residential Lawn Care in Wentzville, MO"
- Cloudflare Pages build log shows Node.js 22.x
- Cloudflare Auto Minify for HTML is disabled
- Phase 1 complete — ready for Phase 2 (Layout Shell + SEO)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template.

Include the live `.pages.dev` URL in the summary for Phase 2 reference.
</output>
