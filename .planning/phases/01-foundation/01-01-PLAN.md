---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - .nvmrc
  - src/lib/constants.ts
  - src/pages/index.astro
  - src/layouts/Layout.astro
  - src/styles/global.css
autonomous: true
requirements:
  - FOUN-01
  - FOUN-05

must_haves:
  truths:
    - "`npm run build` completes without errors and `dist/index.html` exists"
    - "`npx astro check` exits 0 (TypeScript strict, no `any` errors)"
    - "`src/lib/constants.ts` is the sole location of NAP data — no business name, phone, or address appears in any other file"
    - "Project uses Astro 6.1.3, TypeScript strict mode, Node.js 22"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency manifest"
      contains: "astro.*6.1.3"
    - path: "astro.config.mjs"
      provides: "Astro static output config"
      contains: "output: 'static'"
    - path: "tsconfig.json"
      provides: "TypeScript strict config"
      contains: "astro/tsconfigs/strict"
    - path: ".nvmrc"
      provides: "Node version pin"
      contains: "22"
    - path: "src/lib/constants.ts"
      provides: "Canonical NAP single source of truth"
      exports: ["NAP", "SITE_URL", "SOCIAL"]
    - path: "src/pages/index.astro"
      provides: "Root page (placeholder)"
    - path: "src/layouts/Layout.astro"
      provides: "HTML shell with slot"
    - path: "src/styles/global.css"
      provides: "Global CSS entry"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/Layout.astro"
      via: "import Layout"
      pattern: "import Layout from"
    - from: "src/layouts/Layout.astro"
      to: "src/styles/global.css"
      via: "import styles"
      pattern: "import.*global.css"
---

<objective>
Scaffold the Astro 6 project with TypeScript strict mode, pinned dependencies, and the canonical NAP constants file. This is the greenfield starting point — everything in later plans depends on this scaffold existing and building clean.

Purpose: Establish a working Astro 6 static project that `npm run build` can execute without errors. All future phases build on top of this scaffold.
Output: A building Astro project with correct package versions, tsconfig strict mode, and `lib/constants.ts` as the single source of truth for business data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 6 project with pinned dependencies</name>

  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (standard_stack table — exact version pins)
    - CLAUDE.md (TypeScript strict, no `any`)
  </read_first>

  <files>
    package.json, astro.config.mjs, tsconfig.json, .nvmrc, src/pages/index.astro, src/layouts/Layout.astro, src/styles/global.css
  </files>

  <action>
Run `npm create astro@latest . -- --template minimal --yes --no-git` from the project root to scaffold the minimal template.

Then pin exact versions for volatile packages:
```
npm install --save-exact astro@6.1.3
npm install --save-exact tailwindcss@4.2.2 @tailwindcss/vite@4.2.2
npm install @astrojs/react @astrojs/sitemap
npm install --save-exact react@19.2.4 react-dom@19.2.4
npm install sharp
npm install --save-exact @vapi-ai/web@2.5.2
npm install lucide-react
```

After installing, rewrite `astro.config.mjs` to a minimal static-output config (Tailwind/fonts/full config comes in Plan 02):

```typescript
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://redbirdlawnservice.com',
  output: 'static',
  integrations: [
    react(),
    sitemap(),
  ],
});
```

Overwrite `tsconfig.json` with strict config:

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "strictNullChecks": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

Create `.nvmrc` in project root containing only:
```
22
```

Create `src/layouts/Layout.astro` (minimal shell — Font components added in Plan 02):

```astro
---
import '../styles/global.css';
interface Props { title: string; }
const { title } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
```

Create `src/styles/global.css` (Tailwind import added in Plan 02 — placeholder only):

```css
/* Global styles — Tailwind import added in Plan 02 */
:root {
  box-sizing: border-box;
}
```

Replace the generated `src/pages/index.astro` with:

```astro
---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Redbird Lawn Care Service | Wentzville, MO">
  <main>
    <h1>Redbird Lawn Care Service</h1>
    <p>Wentzville, MO — scaffold placeholder</p>
  </main>
</Layout>
```

Run `npm run build` and confirm it exits 0 with `dist/index.html` present.
  </action>

  <verify>
    <automated>
      cd "C:/Users/stlto/My Drive (it@redbirdlawnservice.com)/Website Build" && npm run build && test -f dist/index.html && echo "BUILD OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits with code 0 — no errors in output
    - `dist/index.html` exists after build
    - `package.json` contains `"astro": "6.1.3"` (exact version, no caret)
    - `package.json` contains `"tailwindcss": "4.2.2"`
    - `package.json` contains `"@tailwindcss/vite": "4.2.2"`
    - `package.json` contains `"@vapi-ai/web": "2.5.2"`
    - `package.json` does NOT contain `"@astrojs/tailwind"` (deprecated — must not be present)
    - `astro.config.mjs` contains `output: 'static'`
    - `tsconfig.json` contains `"astro/tsconfigs/strict"`
    - `.nvmrc` contains exactly `22`
    - `src/layouts/Layout.astro` exists and contains `<slot />`
    - `src/styles/global.css` exists
  </acceptance_criteria>

  <done>Project scaffolded, all deps installed at pinned versions, `npm run build` exits 0, `dist/index.html` exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create canonical NAP constants file</name>

  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: NAP Single Source of Truth)
    - .planning/REQUIREMENTS.md (FOUN-05 requirement)
  </read_first>

  <files>
    src/lib/constants.ts
  </files>

  <action>
Create `src/lib/` directory and `src/lib/constants.ts` with the following exact content — no modifications to the values shown (street address and Facebook URL are intentional placeholders to be confirmed before Phase 2):

```typescript
// Canonical NAP — single source of truth for all business data.
// NEVER re-type the phone number, address, or business name in any component.
// Always import from this file.

export const SITE_URL = 'https://redbirdlawnservice.com';

export const NAP = {
  name: 'Redbird Lawn Care Service',
  phone: '(314) 497-6152',
  phoneHref: 'tel:+13144976152',
  email: 'sales@redbirdlawnservic.com',
  address: {
    street: '[CONFIRM FROM GBP]',
    city: 'Wentzville',
    state: 'MO',
    zip: '63385',
    country: 'US',
  },
} as const;

export const SOCIAL = {
  facebook: '[CONFIRM FROM GBP]',
} as const;
```

After writing the file, run `npx astro check` to confirm TypeScript sees no errors.

Do NOT add any NAP data to any other file. The `src/pages/index.astro` placeholder created in Task 1 does not need to import constants — it is a scaffold placeholder.
  </action>

  <verify>
    <automated>
      cd "C:/Users/stlto/My Drive (it@redbirdlawnservice.com)/Website Build" && npx astro check && echo "TS CHECK OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/constants.ts` exists
    - File contains `export const NAP` with `as const`
    - File contains `export const SITE_URL = 'https://redbirdlawnservice.com'`
    - File contains `export const SOCIAL`
    - `NAP.phone` value is exactly `'(314) 497-6152'`
    - `NAP.email` value is exactly `'sales@redbirdlawnservic.com'`
    - `NAP.address.zip` value is exactly `'63385'`
    - `npx astro check` exits 0 (no TypeScript errors)
    - Grep confirms `(314) 497-6152` appears ONLY in `src/lib/constants.ts` — not in any `.astro` or `.tsx` file: `grep -r "497-6152" src/ --include="*.astro" --include="*.tsx" --include="*.ts"` should return only `src/lib/constants.ts`
  </acceptance_criteria>

  <done>`src/lib/constants.ts` exports NAP, SITE_URL, and SOCIAL. `npx astro check` exits 0. Phone number exists only in constants.ts.</done>
</task>

</tasks>

<verification>
Run both verification commands after task completion:
1. `npm run build` — must exit 0, `dist/index.html` must exist
2. `npx astro check` — must exit 0, no TypeScript errors
3. `grep -r "497-6152" src/` — must appear only in `src/lib/constants.ts`
4. `cat package.json | grep -E '"astro"|"tailwindcss"|"@tailwindcss/vite"|"@vapi-ai/web"'` — confirm exact version pins (no `^` prefix on these packages)
</verification>

<success_criteria>
- `npm run build` exits 0 and produces `dist/index.html`
- `npx astro check` exits 0
- `package.json` has astro@6.1.3, tailwindcss@4.2.2, @tailwindcss/vite@4.2.2, @vapi-ai/web@2.5.2 pinned exact
- `@astrojs/tailwind` is NOT in package.json
- `src/lib/constants.ts` is the only file containing `(314) 497-6152` or `Redbird Lawn Care Service`
- `.nvmrc` contains `22`
- `tsconfig.json` extends `astro/tsconfigs/strict`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
