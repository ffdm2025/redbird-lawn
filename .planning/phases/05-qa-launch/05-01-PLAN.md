---
phase: 05-qa-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - public/_headers
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, VAPI-04]

must_haves:
  truths:
    - "npm run build completes without errors"
    - "ContactForm is hydrated with client:idle instead of client:load — deferred until browser idle"
    - "CSP headers include unsafe-eval and VAPI connect-src domains"
    - "PERF-05 JS budget override is documented — VAPI SDK exceeds the original 80KB target by design"
    - "No below-fold images exist that lack lazy loading or WebP format"
    - "REQUIREMENTS.md marks VAPI-04 as complete"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "ContactForm using client:idle hydration"
      contains: "client:idle"
    - path: "public/_headers"
      provides: "CSP with unsafe-eval for VAPI SDK"
      contains: "unsafe-eval"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VAPI-04 marked complete"
      contains: "[x] **VAPI-04**"
  key_links:
    - from: "src/pages/index.astro"
      to: "ContactForm"
      via: "client:idle directive"
      pattern: "ContactForm client:idle"
    - from: "public/_headers"
      to: "VAPI SDK"
      via: "CSP unsafe-eval + connect-src"
      pattern: "unsafe-eval"
---

<objective>
Optimize all controllable performance vectors and document the VAPI SDK JS budget override.

Purpose: Satisfy PERF-01 through PERF-07 and VAPI-04 before the human Lighthouse audit and deploy checkpoint. The only actionable JS optimization is switching ContactForm from client:load to client:idle — everything else (VAPI SDK size, React runtime) is a known architectural tradeoff that must be documented. Images are text/CSS-only, so PERF-07 is satisfied by absence. CSP (VAPI-04) is already written — just needs verification and requirement marked complete.

Output: Optimized index.astro, verified _headers, documented PERF-05 override in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-qa-launch/05-CONTEXT.md
@src/pages/index.astro
@src/layouts/Layout.astro
@public/_headers
</context>

<interfaces>
<!-- Key patterns the executor needs. No external imports — all modifications are to existing files. -->

From src/pages/index.astro (current ContactForm usage):
```astro
<ContactForm client:load />
```
Change to:
```astro
<ContactForm client:idle />
```

From src/layouts/Layout.astro (current VAPIWidget usage — DO NOT CHANGE):
```astro
<VAPIWidget client:only="react" />
```

From public/_headers (current CSP — verify these domains are present):
- script-src: 'unsafe-eval' (required by VAPI/Daily.co WebRTC codec detection)
- connect-src: https://*.vapi.ai wss://*.vapi.ai https://*.daily.co wss://*.daily.co

Astro hydration directives:
- client:load — hydrates immediately on page load (blocking)
- client:idle — defers hydration until browser is idle (requestIdleCallback)
- client:visible — defers until element enters viewport
- client:only="react" — skips SSR entirely, hydrates on client only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Switch ContactForm to client:idle and verify CSP</name>
  <files>src/pages/index.astro, public/_headers</files>
  <action>
    In src/pages/index.astro, change line 45:
    - FROM: `<ContactForm client:load />`
    - TO: `<ContactForm client:idle />`

    Rationale: The contact form is below the fold. client:idle defers React hydration until the browser reports idle, reducing render-blocking JS without any user-visible impact. MobileNav remains client:load in Header.astro (navigation must be ready immediately). VAPIWidget remains client:only="react" in Layout.astro (WebRTC crashes SSR — do not change).

    In public/_headers, verify these exact entries are present (do NOT remove or change):
    - `'unsafe-eval'` in script-src — required by VAPI/Daily.co WebRTC codec detection layer
    - `https://*.vapi.ai wss://*.vapi.ai https://*.daily.co wss://*.daily.co` in connect-src
    - `microphone=(self)` in Permissions-Policy

    If any of those three entries are missing, add them. If all are present, no change needed.

    Run `npm run build` and confirm it exits 0 with no errors.
  </action>
  <verify>
    <automated>cd "C:/Users/stlto/My Drive (it@redbirdlawnservice.com)/Website Build" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build exits 0. src/pages/index.astro contains "client:idle" on the ContactForm line. public/_headers contains unsafe-eval and VAPI domains.</done>
</task>

<task type="auto">
  <name>Task 2: Document PERF-05 JS budget override and mark VAPI-04 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In .planning/REQUIREMENTS.md, make three targeted updates:

    1. Mark VAPI-04 complete — change:
       `- [ ] **VAPI-04**: CSP headers configured with unsafe-eval for VAPI SDK`
       to:
       `- [x] **VAPI-04**: CSP headers configured with unsafe-eval for VAPI SDK`

    2. Annotate PERF-05 with a known-override note — change:
       `- [ ] **PERF-05**: Total homepage < 500KB, JS < 80KB, CSS < 50KB, Fonts < 150KB`
       to:
       `- [ ] **PERF-05**: Total homepage < 500KB, JS < 80KB*, CSS < 50KB, Fonts < 150KB`
       Add a note immediately after the PERF section header (or as inline comment on the line):
       `  <!-- *JS budget override: VAPI SDK (~200KB minified) + React runtime inherently exceed the original 80KB target. This was set before VAPI was a requirement. Total page budget (500KB) remains the binding constraint. ContactForm changed to client:idle to minimize render-blocking JS. -->`

       Actually — REQUIREMENTS.md is a Markdown file, not HTML. Add a blockquote note after the PERF-05 line instead:
       ```
       - [ ] **PERF-05**: Total homepage < 500KB, JS < 80KB*, CSS < 50KB, Fonts < 150KB
         > *JS budget note: @vapi-ai/web SDK (~200KB minified) + React runtime exceed the original 80KB target. Budget set before VAPI was a requirement. Total page < 500KB remains binding. ContactForm changed to client:idle to minimize render-blocking JS.
       ```

    3. In the Traceability table, update VAPI-04 row from Pending to Complete:
       `| VAPI-04 | Phase 4 | Pending |`  →  `| VAPI-04 | Phase 4 | Complete |`

    Read the current file first, make only these three changes, preserve everything else exactly.
  </action>
  <verify>
    <automated>grep -n "VAPI-04\|PERF-05" ".planning/REQUIREMENTS.md"</automated>
  </verify>
  <done>REQUIREMENTS.md shows [x] VAPI-04, PERF-05 line has asterisk annotation, traceability row shows Complete for VAPI-04.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0 — no TypeScript or Astro errors
2. `grep "client:idle" src/pages/index.astro` returns the ContactForm line
3. `grep "unsafe-eval" public/_headers` returns the CSP line
4. `grep "\[x\].*VAPI-04" .planning/REQUIREMENTS.md` returns a match
</verification>

<success_criteria>
- Build passes clean
- ContactForm uses client:idle (reduces render-blocking JS)
- CSP headers verified correct for VAPI (VAPI-04 complete)
- PERF-05 JS budget override documented — VAPI SDK size is a known architectural tradeoff, not a fixable bug
- No images exist below the fold (PERF-07 satisfied by absence — no lazy-loading or format conversions needed)
- PERF-06 (sub-100ms TTFB) is a Cloudflare Pages infrastructure property — verified in Plan 02 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-qa-launch/05-01-SUMMARY.md` following the summary template.
</output>
