---
phase: 04-react-islands-integrations
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CONV-03
  - VAPI-01
  - VAPI-02
  - VAPI-03
  - VAPI-04

must_haves:
  truths:
    - "A homeowner can fill out the contact form and the lead appears in GHL"
    - "Clicking the VAPI button initiates a voice call with Vivian (or shows a clear error if credentials not provisioned)"
    - "Microphone denial shows a graceful fallback with a click-to-call link — no blank screen, no console crash"
    - "The mobile hamburger opens and closes the nav drawer"
    - "No console errors on page load from any of the three React islands"
    - "The page build does not include VAPIWidget HTML in the initial server-rendered output"
  artifacts:
    - path: "src/components/react/ContactForm.tsx"
      provides: "Verified form submitting to GHL"
    - path: "src/components/react/VAPIWidget.tsx"
      provides: "Verified floating voice widget"
    - path: "src/components/react/MobileNav.tsx"
      provides: "Verified mobile drawer"
    - path: "public/_headers"
      provides: "Verified CSP in dist/ output"
  key_links:
    - from: "Browser Network tab"
      to: "GHL webhook"
      via: "ContactForm fetch POST"
      pattern: "200 response to PUBLIC_GHL_WEBHOOK_URL"
---

<objective>
Human verification of all three React islands working end-to-end in the browser.

Purpose: Code correctness is necessary but not sufficient. The VAPI WebRTC call, form submission to GHL, and mobile drawer all require live browser testing to confirm the full flow works — especially VAPI, which behaves differently on localhost vs deployed (CSP only applies on deployed URL).

Output: Signed-off Phase 4 with all five requirements confirmed working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-react-islands-integrations/04-01-SUMMARY.md
@.planning/phases/04-react-islands-integrations/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify env vars and build</name>
  <files></files>
  <read_first>
    - .env (confirm all three vars are set — PUBLIC_VAPI_KEY, PUBLIC_VAPI_ASSISTANT_ID, PUBLIC_GHL_WEBHOOK_URL)
    - .env.example (reference for expected var names)
  </read_first>
  <action>
Run the build and confirm dist/ output is correct:

```bash
npm run build
```

Then verify:
1. `dist/_headers` exists and contains `unsafe-eval`:
   ```bash
   grep "unsafe-eval" dist/_headers
   ```

2. VAPIWidget HTML is NOT in dist/index.html (client:only leaves no server-rendered markup):
   ```bash
   grep -i "vapi\|vivian\|floating" dist/index.html | head -5
   ```
   Expected: no matches (or only inside a script tag, not in HTML body)

3. ContactForm HTML IS in dist/index.html (client:load generates static HTML):
   ```bash
   grep -i "Get a Free Quote\|contact-heading" dist/index.html | head -5
   ```
   Expected: matches (the section heading and form region are in the HTML)

4. Check all three component files exist:
   ```bash
   ls src/components/react/
   ```
   Expected: ContactForm.tsx  MobileNav.tsx  VAPIWidget.tsx

If any check fails, fix the issue before continuing. Document results in SUMMARY.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -E "error|Error|built" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "unsafe-eval" dist/_headers` returns a match
    - `npm run build` exits 0
    - All three .tsx files exist in src/components/react/
  </acceptance_criteria>
  <done>Build verified. dist/ output confirmed correct for deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browser verification of all three islands</name>
  <what-built>
    Three React islands are now wired into the site:
    - MobileNav: hamburger button in header (mobile only), slide-from-right drawer
    - ContactForm: lead capture form in #contact section posting to GHL
    - VAPIWidget: floating red circle bottom-right, voice call with Vivian
    - public/_headers: CSP with unsafe-eval for Cloudflare Pages
  </what-built>
  <how-to-verify>
    Run: `npm run dev` then open http://localhost:4321

    **Check 1 — MobileNav (2 min):**
    1. Resize browser to mobile width (< 768px) or use DevTools device emulation
    2. Confirm a hamburger (3-line) icon appears in the header to the left of the call button
    3. Tap/click the hamburger — drawer should slide in from the right with a dark backdrop
    4. Confirm nav links: Services, About, Reviews, Contact
    5. Tap a nav link — drawer should close and page should scroll to section
    6. Open drawer again — tap the dark backdrop — drawer should close
    7. Open drawer again — tap the X button — drawer should close
    8. Open DevTools Console — no errors during any of these actions

    **Check 2 — ContactForm (3 min):**
    1. Click "Contact" in the nav or scroll to the bottom form section
    2. Click "Get a Free Quote" without filling anything — confirm 4 red inline error messages appear
    3. Fill in: Name=Test User, Address=123 Main St Wentzville MO, Phone=3145550000, Service=Weekly Mowing
    4. Click "Get a Free Quote" — button should show "Sending…" and be disabled
    5a. If GHL URL is provisioned: expect form to be replaced with "Thank you! We'll be in touch within 24 hours." + green checkmark
    5b. If GHL URL is not yet provisioned (undefined): expect error state with "Something went wrong. Please call us at (314) 497-6152" with clickable phone link
    6. Check DevTools Network tab — confirm a POST request was fired (even if it failed with CORS/network error — we want to see the request was made)

    **Check 3 — VAPIWidget (2 min):**
    1. Confirm a red circle button appears bottom-right of the page
    2. Open DevTools Console — no errors on load
    3a. If VAPI keys are provisioned: Click the button — browser should prompt for microphone permission. Allow it — button should pulse and "Speaking with Vivian..." panel should appear. Say "hello" — Vivian should respond. Click "End call".
    3b. If VAPI keys are not yet provisioned: Click the button — expect an error state to appear (not a blank crash). Confirm the mic-denied fallback panel with "Or call us instead" link appears OR a graceful error message appears.
    4. Test mic denied: In browser settings, block microphone for localhost. Click the VAPI button. Confirm "Please enable your microphone to speak with Vivian" message appears with "Or call us instead" link.

    **Check 4 — No console errors (1 min):**
    1. Hard reload the page (Ctrl+Shift+R)
    2. Open DevTools Console
    3. Confirm no red error messages on initial load
    4. Warnings are acceptable; errors are not

    For each check, report: PASS / FAIL / PARTIAL + any issues found.
  </how-to-verify>
  <resume-signal>
    Describe results for each check. Example:
    "Check 1 PASS, Check 2 PASS (GHL not provisioned but error state worked), Check 3 PARTIAL (VAPI keys not provisioned — mic test pending), Check 4 PASS"
    Or describe specific failures so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when all five roadmap requirements are satisfied:

| Requirement | Verification |
|-------------|-------------|
| CONV-03 | ContactForm submits to GHL webhook, lead appears in GHL pipeline (or error state shown gracefully if URL not provisioned) |
| VAPI-01 | VAPIWidget renders, clicking initiates voice call with Vivian |
| VAPI-02 | `client:only="react"` confirmed — `grep "client:only" src/layouts/Layout.astro` matches VAPIWidget |
| VAPI-03 | Microphone denial shows fallback panel with click-to-call link, no console crash |
| VAPI-04 | `dist/_headers` contains `unsafe-eval` in CSP script-src |
</verification>

<success_criteria>
1. All four human verification checks pass (or PARTIAL on credential-gated checks with a clear path to completion)
2. No console errors on page load
3. `dist/_headers` confirmed in dist/ output
4. VAPIWidget confirmed as client:only (no SSR HTML in dist/index.html)
5. Phase 4 ROADMAP entry updated: marked complete with date
</success_criteria>

<output>
After completion, create `.planning/phases/04-react-islands-integrations/04-03-SUMMARY.md`

Update ROADMAP.md Phase 4 status to complete.
Update STATE.md: current focus → Phase 5, Phase 4 marked complete.
</output>
